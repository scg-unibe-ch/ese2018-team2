{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "postgres",
  "password": "",
  "database": "postgres",
  "synchronize": true,
  "logging": false,
  "entities": ["lib/**/*.ts"],
  "migrations": ["migration/**/*.ts"],
  "cli": {
    "migrationsDir": "migration"
  }
}
